name: placeos-analytics
version: 0.1.0

dependencies:
  action-controller:
    github: spider-gazelle/action-controller
    version: "~> 2.0"
  active-model:
    github: spider-gazelle/active-model
  kilt:
    github: jeromegn/kilt
  flux:
    github: place-labs/flux

targets:
  app:
    main: src/app.cr
